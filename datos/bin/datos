DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source "$DIR"/../conf/environment.sh
DATOS_HOME="$DIR"/..
export DATOS_HOME


java  $JVM_OPTS -cp $ASSEMBLY_JAR_PATH com.distribuit.datos.Controller
